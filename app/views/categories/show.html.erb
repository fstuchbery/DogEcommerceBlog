<h1><%= @category.category_name %> Products</h1>
<div id = "categoryShowBody">
<ul>
  <% @products.each do |product| %>
    <li>
      <%= link_to product.product_name, product_path(product) %>
    </li>
  <% end %>
</ul>
</div>